What is this project?
This project is a sample to create a core data database.

What is in this project?
- The table views are loaded with new values as an when the tableview is pulled.
- New set of record data is written into the DB and the using NSFetched results view controller, the changes to the managed object context instance is observed and the tableview is updated accordingly
